# Multi-project mode

Artifacts are namespaced by project:

- `projects/<project>/.ai-agency/docs/...`

Router selects `project` (default: `default`).

Create projects by copying the skeleton:

```bash
cp -R projects/_skeleton projects/<project>
```
